<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sungjukNS">
	<select id="selectSungjukById" parameterType="String" resultType="Sungjuk">
		select * from sungjuk where hakbun = #{hakbun}
	</select>
	
	<insert id="insertSungjuk" parameterType="Sungjuk"> <!-- 헷갈리는 point -->
		insert into sungjuk (hakbun,irum,kor,eng,math)
		 values(#{hakbun},#{irum},#{kor, javaType=integer},#{eng, javaType=integer},#{math, javaType=integer})
	</insert>
	
	<update id="updateSungjuk" parameterType="Sungjuk">
		update sungjuk set kor=#{kor,javaType=integer}, eng=#{eng,javaType=integer}, math=#{math,javaType=integer}
		where hakbun=#{hakbun}
	</update>
	
	<delete id="deleteSungjuk" parameterType="Sungjuk">
		delete from sungjuk where hakbun=#{hakbun}
	</delete>
</mapper>